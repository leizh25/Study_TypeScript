

export { }
/* 

/// <reference path="../../types/global.d.ts" />
    三斜线指令   用来将d.ts文件和当前文件进行关联, 使用的比较少了

    声明文件(d.ts)
        - 声明文件就是专门用来存放声明的文件   以d.ts结尾
        - 在声明文件中可以省略declare
*/
hello.charAt(0)
sayHello()
